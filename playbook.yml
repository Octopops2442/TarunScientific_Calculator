---
- name: Pull docker image of Calculator
  hosts: all
  tasks:
    - name: pull docker image
      shell: docker pull octopops24/calc

    - name: running container
      shell: docker run -it -d octopops24/calc
    
    - name: running the snapshot
      shell: java -jar TarunScientific_Calculator-1.0-SNAPSHOT.jar
